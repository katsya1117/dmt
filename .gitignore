# Dependencies
node_modules/

# Build output
dist/
build/
storybook-static/

# Test / coverage
coverage/

# Environment / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
.idea/
*.swp

# Vite
.vite/
*.local

# Local demo DB (SQLite)
server/data/

# 客先の実際の通信境界(DB接続情報・ネットワーク構成)を含むため非公開。
# ローカル動作確認には docs/legacy-amfphp/webService/config.php（ダミー値）を使う
docs/legacy-amfphp/webService/config.real.php

# tsoa generated (routes/spec) — dev/buildで自動生成
server/src/generated/

# client/serverともyarn運用（yarn.lockが正）。npmで誤って生成されたロックファイルは無視
client/package-lock.json
server/package-lock.json
