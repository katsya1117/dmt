{
  "name": "maintenance",
  "private": true,
  "scripts": {
    "gen:api": "npm --prefix server run tsoa && yarn --cwd client gen:types",
    "dev": "npm run gen:api && concurrently -n client,server -c blue,green \"yarn --cwd client dev\" \"npm --prefix server run dev\"",
    "dev:client": "yarn --cwd client dev",
    "dev:server": "npm --prefix server run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
